model:
  name: nnunet
  configuration: 3d_fullres
  fold: 0
  plans_identifier: nnUNetPlans

inference:
  batch_size: 1
  tile_step_size: 0.5

notes: |
  nnUNet requires separate preprocessing via nnUNetv2.
  This wrapper integrates trained nnUNet checkpoints.
