model:
  name: attention_unet
  in_channels: 4
  out_channels: 4
  spatial_dims: 3
  features: [32, 64, 128, 256, 512]
  dropout: 0.1
  attention_type: additive

inference:
  batch_size: 1
  sliding_window: true
  overlap: 0.5
